%h1 Login

= form_tag login_create_path do

   = label :user, :user_id, 'User-ID'
   = text_field :user, :user_id, :id => "signup_userid"

   = label :user, :email, 'Email'
   = text_field :user, :email, :id => "signup_email"

   = submit_tag 'Login to my account', :id => "signup_submit"

   = link_to "Sign-up for an account", new_user_path
